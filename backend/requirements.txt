fastapi==0.115.0
uvicorn[standard]==0.32.0
sqlalchemy==2.0.36
pydantic==2.9.2
python-multipart==0.0.12
feedparser==6.0.11
httpx==0.27.2
python-dateutil==2.9.0
aiofiles==24.1.0
python-dotenv==1.0.1
openai>=1.35.0
anthropic>=0.28.0
apscheduler>=3.10.4,<4
trafilatura>=1.12.0
praw>=7.7.0
pytrends>=4.9.0
# pytrends 4.9.x passes the legacy `method_whitelist` arg to urllib3, which was
# removed in urllib3 2.0 — pin below 2 so Google Trends collection keeps working.
urllib3<2
# Used only by the optional BigQuery historical-backfill path
# (app/services/gdelt_bigquery.py). Safe to omit if you only use the
# GDELT DOC API + RSS ingestion paths — the import is lazy.
google-cloud-bigquery>=3.0
readability-lxml==0.8.4.1
# Schema migration tooling — added 2026-05-27 for the SQLite → Postgres migration.
# See POSTGRES_MIGRATION_PLAN.md for the phased plan. Both packages are dialect-agnostic
# (alembic works against either; psycopg is only used once DATABASE_URL points at Postgres).
alembic==1.13.3
psycopg[binary]==3.2.3

# Packages that were installed ad-hoc into the previous venv but never added
# to requirements.txt. Reconstituted 2026-05-27 during the venv recreation that
# fixed stale shebangs left over from the project move
# (/Users/theo/campaign-war-room/ → /Users/theo/noctua/).
# Versions match the prior install so behavior stays identical.

# Clustering / embeddings (frame_variants, landscape, topic regions)
hdbscan==0.8.43
umap-learn==0.5.12
scikit-learn==1.8.0
scipy==1.17.1

# Gemini API (embeddings provider)
google-generativeai==0.8.6
google-ai-generativelanguage==0.6.15
google-api-python-client==2.196.0
google-auth-httplib2==0.4.0
httplib2==0.31.2
uritemplate==4.2.0

# HTML parsing / scraping (ingestion, paywall fallbacks)
beautifulsoup4==4.14.3
cloudscraper==1.2.71

# Decodes Google News redirect URLs (news.google.com/rss/articles/CBMI...)
# into their original publisher URLs without making HTTP calls. Used by
# scripts/resolve_google_news_redirects.py to unblock the ~770 race-relevant
# articles whose source_url is a GN redirect (raw_text was just the title).
googlenewsdecoder==0.1.7

# Test runner
pytest==9.0.3
pytest-asyncio==1.3.0
pytest-timeout==2.4.0
